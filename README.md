### Node / Redis Task Queue
This project is unfinished, do not use it.

## Features
* Delayed tasks
* Optional retries with backoff
* RESTful JSON API
* Asynchronous or synchronous processing
* Batches (maybe)
